1. SELECT nazwa FROM miejsca ORDER BY nazwa ASC;
2. SELECT cena FROM miejsca WHERE nazwa = "Malbork";
3. SELECT nazwa, cena, link_obraz FROM miejsca WHERE link_obraz LIKE "0%";
4. SELECT miejsca.nazwa, ROUND(AVG(wycieczki.liczba_doroslych)) FROM miejsca JOIN wycieczki ON miejsca.id_miejsca = wycieczki.id_miejsca GROUP BY miejsca.nazwa;
5. CREATE TABLE klienci (id_klienta int unsigned PRIMARY KEY AUTO_INCREMENT NOT NULL, imie varchar(20), nazwisko varchar(40), data_ur date);